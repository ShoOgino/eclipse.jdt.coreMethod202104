	public String getVisibilityString(int level) {
		switch (level) {
			case AccPublic:
				return PUBLIC;
			case AccProtected:
				return PROTECTED;
			case AccPrivate:
				return PRIVATE;
			default:
				return DEFAULT;
		}
	}

