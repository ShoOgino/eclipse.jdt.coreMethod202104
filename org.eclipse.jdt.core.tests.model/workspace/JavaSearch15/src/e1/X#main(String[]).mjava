    public static void main(String[] args) {
        for (T t : T.values()) {
            int age = t.age();
            String location = location(t).toString();
            t.setRole(t.isManager());
        }
    }

