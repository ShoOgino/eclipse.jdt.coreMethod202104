    private static Kind getKind(CategorizedProblem problem) {
        Kind kind = Kind.OTHER;
        if(problem.isError()) {
            kind = Kind.ERROR;
        }
        if(problem.isWarning()) {
            kind = Kind.WARNING;
        }
        return kind;
    }

