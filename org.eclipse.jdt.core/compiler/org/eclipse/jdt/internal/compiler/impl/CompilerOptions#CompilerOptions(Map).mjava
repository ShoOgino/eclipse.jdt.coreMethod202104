	/**
	 * Initializing the compiler options with external settings
	 * @param settings
	 */
	public CompilerOptions(Map settings){
		resetDefaults();
		if (settings != null) {
			set(settings);
		}
	}

