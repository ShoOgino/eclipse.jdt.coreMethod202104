 	public boolean dependsOn(ModuleBinding other) {
 		if (other == this)
 			return true;
		for (ModuleBinding ref : getAllRequiredModules()) {
			if (ref == other)
				return true;
		}
		return false;
	}

