	public String toString() {
		String s = ""; //$NON-NLS-1$
		int value;
		for (int i = 0, length = valueTable.length; i < length; i++)
			if ((value = valueTable[i]) != -1)
				s += keyTable[i] + " -> " + value + "\n"; 	//$NON-NLS-2$ //$NON-NLS-1$
		return s;
	}

