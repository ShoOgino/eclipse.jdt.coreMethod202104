public String toString() {
	String s = ""; //$NON-NLS-1$
	char[] object;
	for (int i = 0, l = values.length; i < l; i++)
		if ((object = values[i]) != null)
			s += new String(object) + "\n"; //$NON-NLS-1$
	return s;
}

