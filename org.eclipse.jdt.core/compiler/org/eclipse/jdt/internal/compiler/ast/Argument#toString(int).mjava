public String toString(int tab){
	/* slow code */
	
	String s = "";
	if (modifiers != AccDefault){
		s += modifiersString(modifiers);
	}
	if (type == null){
		s += "<no type> ";
	} else {
		s += type.toString(tab) + " ";
	}
	s += new String(name);
	return s;
}

