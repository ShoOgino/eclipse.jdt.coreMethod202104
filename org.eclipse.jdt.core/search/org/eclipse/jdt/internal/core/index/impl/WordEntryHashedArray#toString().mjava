public String toString() {
	String s = ""; //$NON-NLS-1$
	WordEntry entry;
	for (int i = 0, length = elements.length; i < length; i++)
		if ((entry = elements[i]) != null)
			s += entry.toString() + "\n"; 	//$NON-NLS-1$
	return s;
}

