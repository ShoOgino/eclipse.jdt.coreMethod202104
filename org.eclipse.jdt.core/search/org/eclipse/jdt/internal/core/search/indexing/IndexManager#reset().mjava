	/**
	 * Flush current state
	 */
	public void reset() {

		super.reset();
		if (indexes != null) {
			indexes = new Hashtable(5);
			monitors = new Hashtable(5);
		}
		javaPluginLocation = null;
	}

