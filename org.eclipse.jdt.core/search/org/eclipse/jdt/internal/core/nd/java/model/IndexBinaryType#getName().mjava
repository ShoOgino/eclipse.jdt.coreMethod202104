	@Override
	public char[] getName() {
		try (IReader rl = this.typeRef.lock()) {
			NdType type = this.typeRef.get();
			if (type != null) {
				return type.getTypeId().getBinaryName();
			} else {
				return new char[0];
			}
		}
	}

