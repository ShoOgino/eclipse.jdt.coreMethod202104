	void clear(final long offset, final int length) {
		assert this.fLocked;
		this.fDirty= true;
		int idx = recPtrToIndex(offset);
		final int end = idx + length;
		for (; idx < end; idx++) {
			this.fBuffer[idx] = 0;
		}
	}

