	@Override
	public boolean isLocal() {
		try (IReader rl = this.typeRef.lock()) {
			NdType type = this.typeRef.get();
			if (type != null) {
				return type.isLocal();
			} else {
				return false;
			}
		}
	}

