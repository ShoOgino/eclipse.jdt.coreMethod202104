public void cleanup() {
	if (zipFile != null && this.closeZipFileAtEnd) {
		try {
			zipFile.close();
		} catch(IOException e) { // ignore it
		}
		this.zipFile = null;
	}
	this.knownPackageNames = null;
}

