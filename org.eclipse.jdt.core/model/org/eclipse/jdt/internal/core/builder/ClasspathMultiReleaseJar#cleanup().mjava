	@Override
	public void cleanup() {
		if (this.fs != null && this.fs.isOpen()) {
			try {
				this.fs.close();
			} catch (IOException e) {
				// probably already closed, race condition may be?
			}
		}
		super.cleanup();
	}

