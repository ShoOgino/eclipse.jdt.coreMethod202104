	/**
	 * For debugging only
	 */
	public String toString() {
		StringBuffer buf = new StringBuffer("ReferenceInfo(");
		for (int i = 0; i < fNames.length; i++) {
			buf.append(fNames[i]);
			buf.append(" ");
		}
		buf.append(")");
		return buf.toString();
	}

