/**
 * @see IOpenable
 */
public void open(IProgressMonitor pm) throws JavaModelException {
	if (!isOpen()) {
		// TODO: (jerome) need to synchronize (IOpenable.open(IProgressMonitor) is API
		// TODO: (jerome) could use getElementInfo instead
		this.openWhenClosed(pm);
	}
}

