	public String toString() {

		String s = "Pb(" + (id & IgnoreCategoriesMask) + ") ";
		if (message != null) {
			s += message;
		} else {
			if (arguments != null)
				for (int i = 0; i < arguments.length; i++)
					s += " " + arguments[i];
		}
		return s;
	}

