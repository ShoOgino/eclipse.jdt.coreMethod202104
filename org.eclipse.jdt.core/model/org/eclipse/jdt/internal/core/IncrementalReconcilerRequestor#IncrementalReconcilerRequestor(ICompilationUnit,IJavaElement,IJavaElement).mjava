	/**
	 * Creates a requestor
	 */
	public IncrementalReconcilerRequestor(
		ICompilationUnit cu,
		IJavaElement first,
		IJavaElement last) {
		fCompilationUnit = cu;
		fStack = new Stack();
		initializeStack();
		advanceTo(first);
		fLastElement = last;
	}

