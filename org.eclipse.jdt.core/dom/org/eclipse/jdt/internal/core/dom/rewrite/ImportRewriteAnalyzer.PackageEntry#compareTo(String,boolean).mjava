		public int compareTo(String name, boolean isStatic) {
			int cmp= this.packageName.compareTo(name);
			if (cmp == 0) {
				if (this.isForStatic == isStatic) {
					return 0;
				}
				return this.isForStatic ? -1 : 1;
			}
			return cmp;
		}

