		public int compareTo(String fullName, boolean isStatic) {
			int cmp= this.elementName.compareTo(fullName);
			if (cmp == 0) {
				if (this.isStaticImport == isStatic) {
					return 0;
				}
				return this.isStaticImport ? -1 : 1;
			}
			return cmp;
		}

