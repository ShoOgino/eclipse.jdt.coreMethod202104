		public boolean isSameGroup(PackageEntry other) {
			if (this.groupName == null) {
				return other.getGroupID() == null;
			} else {
				return this.groupName.equals(other.getGroupID()) && (this.isForStatic == other.isStatic());
			}
		}		

