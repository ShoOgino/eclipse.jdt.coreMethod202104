	boolean isAtTypeEnd() {
		char currentChar;
		return 
			this.index == -1
			|| this.index >= this.source.length-1 
			|| (currentChar = this.source[this.index]) == ',' 
			|| currentChar == '(' 
			|| currentChar == '<' 
			|| currentChar == ':';
	}

