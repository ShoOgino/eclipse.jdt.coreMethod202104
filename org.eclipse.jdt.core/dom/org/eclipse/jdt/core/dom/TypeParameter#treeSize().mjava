	/* (omit javadoc for this method)
	 * Method declared on ASTNode.
	 */
	int treeSize() {
		return 
			memSize()
			+ (typeVariableName == null ? 0 : getName().treeSize())
			+ typeBounds.listSize();
	}

