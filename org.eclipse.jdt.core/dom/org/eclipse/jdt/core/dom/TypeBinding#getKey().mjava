	/*
	 * @see IBinding#getKey()
	 */
	public String getKey() {
		if (this.key == null) {
			StringBuffer buffer = new StringBuffer();
			appendKey(buffer);
			this.key = buffer.toString();
		}
		return this.key;
	}

