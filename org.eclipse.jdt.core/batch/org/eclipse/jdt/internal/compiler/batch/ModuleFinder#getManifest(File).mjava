	private static Manifest getManifest(File file) {
		if (!isJar(file))
			return null;
		try (JarFile jar = new JarFile(file)) {
			return jar.getManifest();
		} catch (IOException e) {
			return null;
		}
	}

