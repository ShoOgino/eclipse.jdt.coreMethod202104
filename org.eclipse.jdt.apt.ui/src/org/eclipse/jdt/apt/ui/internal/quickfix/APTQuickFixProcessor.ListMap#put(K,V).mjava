        public void put(K key, V value)
        {
            List<V> list = fData.get(key);
            if (list == null)
            {
                list = new ArrayList<V>();
                fData.put(key, list);
            }
            list.add(value);
        }

